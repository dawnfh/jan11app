<h2>All Users</h2>

<ul>
	<% @users.each do |user| %>
		<li><%= @user.username %></li></br>

<!-- pulls info on specific users
 -->
		<li><%= link_to user.username, user_path(user) %> | &nbsp; &nbsp; &nbsp; |
		<%= link_to "Edit", edit_user_path(user) %><li><br><hr>

	<% end %>
</ul>
